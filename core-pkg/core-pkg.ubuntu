#!/usr/bin/env bash

core_pkg="zsh stow gawk git tmux"
non_deb_pkg="starship"

type $core_pkg && exit 0
apt-get update
apt-get install -y $core_pkg

type $non_deb_pkg && exit 0
sh -c "$(curl -fsSL https://starship.rs/install.sh)"

